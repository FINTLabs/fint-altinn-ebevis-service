scheduling:
  initial-delay: 10000
  fixed-delay: 7200000

altinn:
  base-url: https://api.data.altinn.no/v1
  ocp-apim-subscription-key:
  key-store-password:
  key-store-file:

management:
  endpoints:
    web:
      exposure:
        include: health, shutdown, prometheus
